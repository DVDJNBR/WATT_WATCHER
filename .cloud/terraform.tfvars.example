# GRID_POWER_STREAM — Terraform Variables
# Copy this file to terraform.tfvars and fill in your values.

project_name = "ww"
environment  = "dev"
location     = "francecentral"

# SQL Server credentials
sql_admin_login    = "wwadmin"
sql_admin_password = "CHANGE_ME_Str0ng!Pass"  # ⚠️ Change this!

# Data retention (days)
retention_bronze_days = 180   # 6 months
retention_silver_days = 90    # 3 months
retention_audit_days  = 365   # 12 months
