# WATT WATCHER — Frontend environment variables
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to source control.

# Azure AD App Registration (from Azure Portal → App registrations)
VITE_AZURE_AD_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
VITE_AZURE_AD_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# OAuth redirect URI (must match the redirect URI registered in Azure AD)
# For local dev: http://localhost:5173
VITE_REDIRECT_URI=http://localhost:5173

# API base URL (Azure Functions or Azure API Management endpoint)
# For local dev with Azure Functions: http://localhost:7071/api
# For production: https://<your-func-app>.azurewebsites.net/api
VITE_API_BASE_URL=http://localhost:7071/api
